{
  "name": "sententia-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Sententia — Swiss legal document generation plugin for Claude",
  "scripts": {
    "build:ollama": "cd sententia/mcp-servers/ollama && npm install && npm run build:bundle",
    "package": "bash scripts/package-plugin.sh"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sententiaki/Sententia-plugin"
  },
  "author": "Sententia",
  "license": "AGPL-3.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
